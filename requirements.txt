# Anonymous Studio — Python dependencies
#
# PYTHON VERSION: 3.9, 3.10, 3.11, or 3.12 ONLY
#
# Python 3.13+ is NOT supported with this Taipy range (`taipy>=3.1.0,<4.2`).
# Create your venv with a supported version explicitly:
#
#   python3.12 -m venv .venv        (recommended)
#   python3.11 -m venv .venv
#
# If you only have Python 3.13+ installed, get 3.12 from https://python.org first.
# Check your version: python --version  (must show 3.12.x or similar)

taipy>=3.1.0,<4.2
presidio-analyzer>=2.2.35
presidio-anonymizer>=2.2.35
spacy>=3.7.0,<3.9
pandas>=2.2.0,<4.0.0
openpyxl>=3.1.2,<=3.1.5
chardet<8
faker>=24.0.0
python-dotenv>=1.0.0
PyJWT>=2.8.0,<3
pymongo[srv]>=4.7
mongomock>=0.4
dask[dataframe]>=2024.8.0
duckdb>=1.0.0
openai>=1.40.0
prometheus_client>=0.20.0
schedule>=1.2.0
# Security floor constraints from dependency audit (transitive via taipy stack).
# CVEs remediated: CVE-2024-6866, CVE-2024-6844, CVE-2024-6839, CVE-2025-69534,
# CVE-2025-58367, CVE-2026-33155, CVE-2026-27205, CVE-2025-68480.
flask>=3.1.3
# flask-cors: cannot add a floor >=6.0.0 — taipy-gui 3.1.0 hard-pins flask-cors==4.0.0
markdown>=3.8.1
deepdiff>=8.6.2
marshmallow>=3.26.2,<4
